version: '3'

services:
  morecats:
    build: .
    image: terracecn/morecats
    restart: always
    environment:
      HOST: 0.0.0.0
      CAT_IMAGE_DIR: /var/morecats/images
    volumes:
      - ./cat_images:/var/morecats/images
    ports:
      - 8080:8080
      